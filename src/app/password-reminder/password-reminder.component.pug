div.middle-box.text-center.loginscreen
  div
    div.logo-container
      img.logo(src="assets/images/logo.png")
    h4 Állattenyésztési Teljesítményvizsgáló Kft.
    form.m-t(role="form", #loginForm="ngForm", (ngSubmit)="onSubmit()", [ngClass]="{'has-error': (loginForm.submitted && loginError)}")
      div.form-group
        input#email.form-control(type="email", placeholder="Email cím", name="email", [(ngModel)]="emailAddress", #email="ngModel", required, autofocus, pattern="^[a-zA-Z0-9.!#$%&’*+/=?^_`{|}~-]+@[a-zA-Z0-9-]+.*$")
      p.help-block(*ngIf="loginForm.submitted && loginError")
        span A megadott e-mail cím helytelen!
      p.text-info(*ngIf="loginForm.submitted && loginSuccess")
        span Az e-mailt sikeresen elküldtük!
      button.btn.btn-primary.block.full-width.m-b(type="submit", [disabled]="loginForm.invalid") Új jelszót kérek
      a([routerLink]="'/login'") Bejelentkezés
